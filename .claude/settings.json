{
  "env": {
    "PATH": "C:\\Program Files\\Docker\\Docker\\resources\\bin;C:\\Program Files\\GitHub CLI;${PATH}"
  },
  "permissions": {
    "allow": [
      "Bash(git add *)",
      "Bash(git commit -m ' *)",
      "Bash(git push *)",
      "WebSearch",
      "Bash(./gradlew bootRun)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "$cmd = ($input | ConvertFrom-Json).tool_input.command; if ($cmd -match 'gradlew') { $procs = Get-Process -Name java -ErrorAction SilentlyContinue; if ($procs) { $procs | Stop-Process -Force } }",
            "statusMessage": "既存のJavaプロセスを確認中..."
          }
        ]
      }
    ]
  }
}
