# 画面構成・システム構成

## 画面構成

```
┌─────────────────────────────────────────────┐
│  ボードタイトル                              │
├───────────────┬───────────────┬─────────────┤
│  To Do (2)    │ In Progress(1)│  Done (1)   │
│ ┌───────────┐ │ ┌───────────┐ │ ┌─────────┐ │
│ │ カードA ← │ │ │カードC ←→│ │ │カードD→ │ │
│ └───────────┘ │ └───────────┘ │ └─────────┘ │
│ ┌───────────┐ │               │             │
│ │ カードB ← │ │               │             │
│ └───────────┘ │               │             │
│ + カードを追加│ + カードを追加│+ カードを追加│
└───────────────┴───────────────┴─────────────┘
```

---

## システム構成・データフロー

```
[ユーザー (ブラウザ)]
      │  HTTPS
      ▼
[CloudFront + S3]         ← React (SPA) を配信
      │
      │ REST API (HTTPS)
      ▼
[EC2 : Spring Boot]       ← ビジネスロジック・バリデーション
      │
      │ JDBC / JPA
      ▼
[RDS : PostgreSQL]        ← データ永続化
```
